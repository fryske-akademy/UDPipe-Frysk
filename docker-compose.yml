version: "3.7"
services:
    udpipe-frysk:
       image: "${APPNAME}:${VERSION}"
       deploy:
          replicas: 1
          resources:
             limits:
                memory: "3GB"
       ports:
          - "10007:3838"
       networks:
          - ${APPNAME}
networks:
    udpipe-frysk:
